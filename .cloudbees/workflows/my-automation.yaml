apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation
on:
  push:
    branches:
      - "**"
jobs:
  Checkout:
    steps:
      - name: checkout
        uses: cloudbees-io/checkout@v1
  Build:
    needs: Checkout
    steps:
      - uses: docker://alpine/git:latest
        run: |
          git config --global --add safe.directory /cloudbees/workspace
      - name: checkout
        uses: cloudbees-io/checkout@v1
      - name: Build
        uses: docker://maven:3-eclipse-temurin-17
        run: |
          mvn clean install -DskipTests=true
  
      

      - name: Configure AWS credentials
        uses: cloudbees-io/configure-aws-credentials@v1
        with:
          aws-access-key-id: ASIAT43X4R4GCNGIHQGS
          aws-secret-access-key: jF6zxW/xX/JCky6jye9qXAjVOhFL41Mp5+vXTnFS
          aws-region: us-east-1
      
      

      - name: Create and use EC2 key pair
        uses: docker://amazon/aws-cli:latest
        run: |
          ls
          aws ec2 create-key-pair --key-name petclinic --region us-east-1 --query 'KeyMaterial' --output text > petclinic.pem
          chmod 400 petclinic.pem
          aws ec2 run-instances --image-id ami-04b70fa74e45c3917 --instance-type t2.micro --key-name "petclinic" --region us-east-1

             